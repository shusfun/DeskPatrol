# AGENTS.md

- 只使用简体中文沟通和编写新增文档。
- 修改依赖、锁文件、workspace、运行时、构建或部署入口前后，必须运行严格仓库基础审计。
- macOS 禁止启动 Docker；构建、测试和长驻服务必须经过设备资源守卫。
- 禁止 fallback、mock fallback 和静默降级；失败必须返回真实原因。
- 未经用户明确要求，不启动浏览器或 Playwright 验证。
- 不触碰与当前任务无关的用户改动。
